<%- include( "../partials/header.ejs" ) %>

    <% if( currentUser && currentUser.isAdmin ){ %>
        <body class="bgAllPage">
            <!-- date -->
            <div class=" text-center text-light mt-3">
                <% var today = new Date() %> 
                <% var date = today.getDate() + ' ' + (today.toLocaleString('default', { month: 'long' })) + ' ' + today.getFullYear() %>
                <h4> <%= date %> </h4>
            </div>
            
                <!-- cinema1 -->
                <% let x = 0 %>
                <% cinema.forEach(function(cinema){ %>
                    <% if( Date.now() >= Date.parse(cinema.startDate) &&  Date.now() <= Date.parse(cinema.endDate)) {%>
                        <% if(x === 0 ){ x = 1 %>
                                <h4 class="text-light ms-5"> Cinema 1 </h4>
                        <% } %>    
                        <% if( cinema.cinema === '1' ){ %> 
                            <div class="container">
                                <ol class="list-group w-50">
                                    <% movie.forEach(function(movie){ %>
                                        <% if( movie._id.equals(cinema.movieId._id)){ %>
                                            <li class="list-group-item d-flex justify-content-between align-items-start mt-3 bgAllPage text-light">
                                                <img src="<%= movie.poster %>" width="150" height="200">
                                                <div class="ms-2 me-auto mt-4">
                                                    <h5 class="card-title ms-3"><%= movie.name %></h5>
                                                    <p class="card-text ms-3"> <%= movie.genre %> </p>
                                                    <p class="card-text ms-3"> <%= movie.rate %> </p>
                                                    <p class="card-text ms-3"> <%= movie.time %> </p>
                                                    <a href = "/movies/<%= movie._id %>" class = "btn btn-sm btn-outline-light ms-3" > VIEW </a>
                                                </div>
                                            </li>

                                            <div class="d-flex mt-3">
                                                <% for( let i = 0; cinema.allShowtime[i] != null; i++){ %> 
                                                    <form action="/select-seat/<%= cinema._id %>/<%= movie._id %>" method = "post">
                                                        <input type="hidden" name = "showtime" value="<%= cinema.allShowtime[i] %>">
                                                        <% let time = cinema.allShowtime[i].split(':') %>       
                                                        <% if( Number(time[0]) <= today.getHours() && Number(time[1]) <= today.getMinutes()){ %> 
                                                        <button type="submit" disabled class="btn card-text btn btn-outline-warning me-5 mt-2 mb-2"> <%= cinema.allShowtime[i] %></button>
                                                        <% } else { %>
                                                        <button type="submit" class="btn card-text btn btn-outline-warning me-5 mt-2 mb-2"> <%= cinema.allShowtime[i] %></button>
                                                        <% } %>
                                                        <input type="hidden" name = "date" value="<%= today.getFullYear() + '-' + Number(today.getMonth()+1) + '-' + today.getDate() %>"> 
                                                    </form>
                                                <% } %>
                                            </div>
                                        <% } %>
                                    <% }) %>
                                </ol>
                            </div>
                        <% } %>
                        <% } %> 
                <% }) %>

                <!-- cinema2 -->
                <% cinema.forEach(function(cinema){ %>
                    <% if( Date.now() >= Date.parse(cinema.startDate) &&  Date.now() <= Date.parse(cinema.endDate)) {%>
                        <% if(x === 1 ){ x = 2 %>
                                <h4 class="text-light ms-5"> Cinema 2 </h4>
                        <% } %>    
                        <% if( cinema.cinema === '2' ){ %> 
                            <div class="container">
                                <ol class="list-group w-50">
                                    <% movie.forEach(function(movie){ %>
                                        <% if( movie._id.equals(cinema.movieId._id)){ %>
                                            <li class="list-group-item d-flex justify-content-between align-items-start mt-3 bgAllPage text-light">
                                                <img src="<%= movie.poster %>" width="150" height="200">
                                                <div class="ms-2 me-auto mt-4">
                                                    <h5 class="card-title ms-3"><%= movie.name %></h5>
                                                    <p class="card-text ms-3"> <%= movie.genre %> </p>
                                                    <p class="card-text ms-3"> <%= movie.rate %> </p>
                                                    <p class="card-text ms-3"> <%= movie.time %> </p>
                                                    <a href = "/movies/<%= movie._id %>" class = "btn btn-sm btn-outline-light ms-3" > VIEW </a>
                                                </div>
                                            </li>

                                            <div class="d-flex mt-3">
                                                <% for( let i = 0; cinema.allShowtime[i] != null; i++){ %> 
                                                    <form action="/select-seat/<%= cinema._id %>/<%= movie._id %>" method = "post">
                                                        <input type="hidden" name = "showtime" value="<%= cinema.allShowtime[i] %>">
                                                        <% let time = cinema.allShowtime[i].split(':') %>       
                                                        <% if( Number(time[0]) <= today.getHours() && Number(time[1]) <= today.getMinutes()){ %> 
                                                        <button type="submit" disabled class="btn card-text btn btn-outline-warning me-5 mt-2 mb-2"> <%= cinema.allShowtime[i] %></button>
                                                        <% } else { %>
                                                        <button type="submit" class="btn card-text btn btn-outline-warning me-5 mt-2 mb-2"> <%= cinema.allShowtime[i] %></button>
                                                        <% } %>
                                                        <input type="hidden" name = "date" value="<%= today.getFullYear() + '-' + Number(today.getMonth()+1) + '-' + today.getDate() %>"> 
                                                    </form>
                                                <% } %>
                                            </div>
                                        <% } %>
                                    <% }) %>
                                </ol>
                            </div>
                        <% } %>
                        <% } %> 
                <% }) %>

                 <!-- cinema3 -->
                 <% cinema.forEach(function(cinema){ %>
                    <% if( Date.now() >= Date.parse(cinema.startDate) &&  Date.now() <= Date.parse(cinema.endDate)) {%>
                        <% if(x === 2 ){ x = 3 %>
                                <h4 class="text-light ms-5"> Cinema 3 </h4>
                        <% } %>    
                        <% if( cinema.cinema === '3' ){ %> 
                            <div class="container">
                                <ol class="list-group w-50">
                                    <% movie.forEach(function(movie){ %>
                                        <% if( movie._id.equals(cinema.movieId._id)){ %>
                                            <li class="list-group-item d-flex justify-content-between align-items-start mt-3 bgAllPage text-light">
                                                <img src="<%= movie.poster %>" width="150" height="200">
                                                <div class="ms-2 me-auto mt-4">
                                                    <h5 class="card-title ms-3"><%= movie.name %></h5>
                                                    <p class="card-text ms-3"> <%= movie.genre %> </p>
                                                    <p class="card-text ms-3"> <%= movie.rate %> </p>
                                                    <p class="card-text ms-3"> <%= movie.time %> </p>
                                                    <a href = "/movies/<%= movie._id %>" class = "btn btn-sm btn-outline-light ms-3" > VIEW </a>
                                                </div>
                                            </li>

                                            <div class="d-flex mt-3">
                                                <% for( let i = 0; cinema.allShowtime[i] != null; i++){ %> 
                                                    <form action="/select-seat/<%= cinema._id %>/<%= movie._id %>" method = "post">
                                                        <input type="hidden" name = "showtime" value="<%= cinema.allShowtime[i] %>">
                                                        <% let time = cinema.allShowtime[i].split(':') %>       
                                                        <% if( Number(time[0]) <= today.getHours() && Number(time[1]) <= today.getMinutes()){ %> 
                                                        <button type="submit" disabled class="btn card-text btn btn-outline-warning me-5 mt-2 mb-2"> <%= cinema.allShowtime[i] %></button>
                                                        <% } else { %>
                                                        <button type="submit" class="btn card-text btn btn-outline-warning me-5 mt-2 mb-2"> <%= cinema.allShowtime[i] %></button>
                                                        <% } %>
                                                        <input type="hidden" name = "date" value="<%= today.getFullYear() + '-' + Number(today.getMonth()+1) + '-' + today.getDate() %>"> 
                                                    </form>
                                                <% } %>
                                            </div>
                                        <% } %>
                                    <% }) %>
                                </ol>
                            </div>
                        <% } %>
                        <% } %> 
                <% }) %>

                 <!-- cinema4 -->
                 <% cinema.forEach(function(cinema){ %>
                    <% if( Date.now() >= Date.parse(cinema.startDate) &&  Date.now() <= Date.parse(cinema.endDate)) {%>
                        <% if(x === 3 ){ x = 4 %>
                                <h4 class="text-light ms-5"> Cinema 4 </h4>
                        <% } %>    
                        <% if( cinema.cinema === '4' ){ %> 
                            <div class="container">
                                <ol class="list-group w-50">
                                    <% movie.forEach(function(movie){ %>
                                        <% if( movie._id.equals(cinema.movieId._id)){ %>
                                            <li class="list-group-item d-flex justify-content-between align-items-start mt-3 bgAllPage text-light">
                                                <img src="<%= movie.poster %>" width="150" height="200">
                                                <div class="ms-2 me-auto mt-4">
                                                    <h5 class="card-title ms-3"><%= movie.name %></h5>
                                                    <p class="card-text ms-3"> <%= movie.genre %> </p>
                                                    <p class="card-text ms-3"> <%= movie.rate %> </p>
                                                    <p class="card-text ms-3"> <%= movie.time %> </p>
                                                    <a href = "/movies/<%= movie._id %>" class = "btn btn-sm btn-outline-light ms-3" > VIEW </a>
                                                </div>
                                            </li>

                                            <div class="d-flex mt-3">
                                                <% for( let i = 0; cinema.allShowtime[i] != null; i++){ %> 
                                                    <form action="/select-seat/<%= cinema._id %>/<%= movie._id %>" method = "post">
                                                        <input type="hidden" name = "showtime" value="<%= cinema.allShowtime[i] %>">
                                                        <% let time = cinema.allShowtime[i].split(':') %>       
                                                        <% if( Number(time[0]) <= today.getHours() && Number(time[1]) <= today.getMinutes()){ %> 
                                                        <button type="submit" disabled class="btn card-text btn btn-outline-warning me-5 mt-2 mb-2"> <%= cinema.allShowtime[i] %></button>
                                                        <% } else { %>
                                                        <button type="submit" class="btn card-text btn btn-outline-warning me-5 mt-2 mb-2"> <%= cinema.allShowtime[i] %></button>
                                                        <% } %>
                                                        <input type="hidden" name = "date" value="<%= today.getFullYear() + '-' + Number(today.getMonth()+1) + '-' + today.getDate() %>"> 
                                                    </form>
                                                <% } %>
                                            </div>
                                        <% } %>
                                    <% }) %>
                                </ol>
                            </div>
                        <% } %>
                        <% } %> 
                <% }) %>

                 <!-- cinema5 -->
                <% cinema.forEach(function(cinema){ %>
                    <% if( Date.now() >= Date.parse(cinema.startDate) &&  Date.now() <= Date.parse(cinema.endDate)) {%>
                        <% if(x === 4 ){ x = 5 %>
                                <h4 class="text-light ms-5"> Cinema 5 </h4>
                        <% } %>    
                        <% if( cinema.cinema === '5' ){ %> 
                            <div class="container">
                                <ol class="list-group w-50">
                                    <% movie.forEach(function(movie){ %>
                                        <% if( movie._id.equals(cinema.movieId._id)){ %>
                                            <li class="list-group-item d-flex justify-content-between align-items-start mt-3 bgAllPage text-light">
                                                <img src="<%= movie.poster %>" width="150" height="200">
                                                <div class="ms-2 me-auto mt-4">
                                                    <h5 class="card-title ms-3"><%= movie.name %></h5>
                                                    <p class="card-text ms-3"> <%= movie.genre %> </p>
                                                    <p class="card-text ms-3"> <%= movie.rate %> </p>
                                                    <p class="card-text ms-3"> <%= movie.time %> </p>
                                                    <a href = "/movies/<%= movie._id %>" class = "btn btn-sm btn-outline-light ms-3" > VIEW </a>
                                                </div>
                                            </li>

                                            <div class="d-flex mt-3">
                                                <% for( let i = 0; cinema.allShowtime[i] != null; i++){ %> 
                                                    <form action="/select-seat/<%= cinema._id %>/<%= movie._id %>" method = "post">
                                                        <input type="hidden" name = "showtime" value="<%= cinema.allShowtime[i] %>">
                                                        <% let time = cinema.allShowtime[i].split(':') %>       
                                                        <% if( Number(time[0]) <= today.getHours() && Number(time[1]) <= today.getMinutes()){ %> 
                                                        <button type="submit" disabled class="btn card-text btn btn-outline-warning me-5 mt-2 mb-2"> <%= cinema.allShowtime[i] %></button>
                                                        <% } else { %>
                                                        <button type="submit" class="btn card-text btn btn-outline-warning me-5 mt-2 mb-2"> <%= cinema.allShowtime[i] %></button>
                                                        <% } %>
                                                        <input type="hidden" name = "date" value="<%= today.getFullYear() + '-' + Number(today.getMonth()+1) + '-' + today.getDate() %>"> 
                                                    </form>
                                                <% } %>
                                            </div>
                                        <% } %>
                                    <% }) %>
                                </ol>
                            </div>
                        <% } %>
                        <% } %> 
                <% }) %>

                <!-- cinema 6 -->
                <% cinema.forEach(function(cinema){ %>
                    <% if( Date.now() >= Date.parse(cinema.startDate) &&  Date.now() <= Date.parse(cinema.endDate)) {%>
                        <% if(x === 5 ){ x = 6 %>
                                <h4 class="text-light ms-5"> Cinema 6 </h4>
                        <% } %>    
                        <% if( cinema.cinema === '6' ){ %> 
                            <div class="container">
                                <ol class="list-group w-50">
                                    <% movie.forEach(function(movie){ %>
                                        <% if( movie._id.equals(cinema.movieId._id)){ %>
                                            <li class="list-group-item d-flex justify-content-between align-items-start mt-3 bgAllPage text-light">
                                                <img src="<%= movie.poster %>" width="150" height="200">
                                                <div class="ms-2 me-auto mt-4">
                                                    <h5 class="card-title ms-3"><%= movie.name %></h5>
                                                    <p class="card-text ms-3"> <%= movie.genre %> </p>
                                                    <p class="card-text ms-3"> <%= movie.rate %> </p>
                                                    <p class="card-text ms-3"> <%= movie.time %> </p>
                                                    <a href = "/movies/<%= movie._id %>" class = "btn btn-sm btn-outline-light ms-3" > VIEW </a>
                                                </div>
                                            </li>

                                            <div class="d-flex mt-3">
                                                <% for( let i = 0; cinema.allShowtime[i] != null; i++){ %> 
                                                    <form action="/select-seat/<%= cinema._id %>/<%= movie._id %>" method = "post">
                                                        <input type="hidden" name = "showtime" value="<%= cinema.allShowtime[i] %>">
                                                        <% let time = cinema.allShowtime[i].split(':') %>       
                                                        <% if( Number(time[0]) <= today.getHours() && Number(time[1]) <= today.getMinutes()){ %> 
                                                        <button type="submit" disabled class="btn card-text btn btn-outline-warning me-5 mt-2 mb-2"> <%= cinema.allShowtime[i] %></button>
                                                        <% } else { %>
                                                        <button type="submit" class="btn card-text btn btn-outline-warning me-5 mt-2 mb-2"> <%= cinema.allShowtime[i] %></button>
                                                        <% } %>
                                                        <input type="hidden" name = "date" value="<%= today.getFullYear() + '-' + Number(today.getMonth()+1) + '-' + today.getDate() %>"> 
                                                    </form>
                                                <% } %>
                                            </div>
                                        <% } %>
                                    <% }) %>
                                </ol>
                            </div>
                        <% } %>
                        <% } %> 
                <% }) %>

                <!-- cinema 7 -->
                <% cinema.forEach(function(cinema){ %>
                    <% if( Date.now() >= Date.parse(cinema.startDate) &&  Date.now() <= Date.parse(cinema.endDate)) {%>
                        <% if(x === 6 ){ x = 7 %>
                                <h4 class="text-light ms-5"> Cinema 7 </h4>
                        <% } %>    
                        <% if( cinema.cinema === '7' ){ %> 
                            <div class="container">
                                <ol class="list-group w-50">
                                    <% movie.forEach(function(movie){ %>
                                        <% if( movie._id.equals(cinema.movieId._id)){ %>
                                            <li class="list-group-item d-flex justify-content-between align-items-start mt-3 bgAllPage text-light">
                                                <img src="<%= movie.poster %>" width="150" height="200">
                                                <div class="ms-2 me-auto mt-4">
                                                    <h5 class="card-title ms-3"><%= movie.name %></h5>
                                                    <p class="card-text ms-3"> <%= movie.genre %> </p>
                                                    <p class="card-text ms-3"> <%= movie.rate %> </p>
                                                    <p class="card-text ms-3"> <%= movie.time %> </p>
                                                    <a href = "/movies/<%= movie._id %>" class = "btn btn-sm btn-outline-light ms-3" > VIEW </a>
                                                </div>
                                            </li>

                                            <div class="d-flex mt-3">
                                                <% for( let i = 0; cinema.allShowtime[i] != null; i++){ %> 
                                                    <form action="/select-seat/<%= cinema._id %>/<%= movie._id %>" method = "post">
                                                        <input type="hidden" name = "showtime" value="<%= cinema.allShowtime[i] %>">
                                                        <% let time = cinema.allShowtime[i].split(':') %>       
                                                        <% if( Number(time[0]) <= today.getHours() && Number(time[1]) <= today.getMinutes()){ %> 
                                                        <button type="submit" disabled class="btn card-text btn btn-outline-warning me-5 mt-2 mb-2"> <%= cinema.allShowtime[i] %></button>
                                                        <% } else { %>
                                                        <button type="submit" class="btn card-text btn btn-outline-warning me-5 mt-2 mb-2"> <%= cinema.allShowtime[i] %></button>
                                                        <% } %>
                                                        <input type="hidden" name = "date" value="<%= today.getFullYear() + '-' + Number(today.getMonth()+1) + '-' + today.getDate() %>"> 
                                                    </form>
                                                <% } %>
                                            </div>
                                        <% } %>
                                    <% }) %>
                                </ol>
                            </div>
                        <% } %>
                        <% } %> 
                <% }) %>

                <!-- cinema 8 -->
                <% cinema.forEach(function(cinema){ %>
                    <% if( Date.now() >= Date.parse(cinema.startDate) &&  Date.now() <= Date.parse(cinema.endDate)) {%>
                        <% if(x === 7 ){ x = 8 %>
                                <h4 class="text-light ms-5"> Cinema 8 </h4>
                        <% } %>    
                        <% if( cinema.cinema === '8' ){ %> 
                            <div class="container">
                                <ol class="list-group w-50">
                                    <% movie.forEach(function(movie){ %>
                                        <% if( movie._id.equals(cinema.movieId._id)){ %>
                                            <li class="list-group-item d-flex justify-content-between align-items-start mt-3 bgAllPage text-light">
                                                <img src="<%= movie.poster %>" width="150" height="200">
                                                <div class="ms-2 me-auto mt-4">
                                                    <h5 class="card-title ms-3"><%= movie.name %></h5>
                                                    <p class="card-text ms-3"> <%= movie.genre %> </p>
                                                    <p class="card-text ms-3"> <%= movie.rate %> </p>
                                                    <p class="card-text ms-3"> <%= movie.time %> </p>
                                                    <a href = "/movies/<%= movie._id %>" class = "btn btn-sm btn-outline-light ms-3" > VIEW </a>
                                                </div>
                                            </li>

                                            <div class="d-flex mt-3">
                                                <% for( let i = 0; cinema.allShowtime[i] != null; i++){ %> 
                                                    <form action="/select-seat/<%= cinema._id %>/<%= movie._id %>" method = "post">
                                                        <input type="hidden" name = "showtime" value="<%= cinema.allShowtime[i] %>">
                                                        <% let time = cinema.allShowtime[i].split(':') %>       
                                                        <% if( Number(time[0]) <= today.getHours() && Number(time[1]) <= today.getMinutes()){ %> 
                                                        <button type="submit" disabled class="btn card-text btn btn-outline-warning me-5 mt-2 mb-2"> <%= cinema.allShowtime[i] %></button>
                                                        <% } else { %>
                                                        <button type="submit" class="btn card-text btn btn-outline-warning me-5 mt-2 mb-2"> <%= cinema.allShowtime[i] %></button>
                                                        <% } %>
                                                        <input type="hidden" name = "date" value="<%= today.getFullYear() + '-' + Number(today.getMonth()+1) + '-' + today.getDate() %>"> 
                                                    </form>
                                                <% } %>
                                            </div>
                                        <% } %>
                                    <% }) %>
                                </ol>
                            </div>
                        <% } %>
                        <% } %> 
                <% }) %>
        

    <% } else { %> 
        <body class="bgAllPage">
            <!-- date -->
            <div class=" text-center text-light mt-3">
                <% var today = new Date() %> 
                <% var date = today.getDate() + ' ' + (today.toLocaleString('default', { month: 'long' })) + ' ' + today.getFullYear() %>
                <h4> <%= date %> </h4>
            </div>
            
            
                <!-- cinema1 -->
                <% let x = 0 %>
                <% cinema.forEach(function(cinema){ %>
                    <% if( Date.now() >= Date.parse(cinema.startDate) &&  Date.now() <= Date.parse(cinema.endDate)) {%>
                        <% if(x === 0 ){ x = 1 %>
                                <h4 class="text-light ms-5"> Cinema 1 </h4>
                        <% } %>    
                        <% if( cinema.cinema === '1' ){ %> 
                            <div class="container">
                                <ol class="list-group w-50">
                                    <% movie.forEach(function(movie){ %>
                                        <% if( movie._id.equals(cinema.movieId._id)){ %>
                                            <li class="list-group-item d-flex justify-content-between align-items-start mt-3 bgAllPage text-light">
                                                <img src="<%= movie.poster %>" width="150" height="200">
                                                <div class="ms-2 me-auto mt-4">
                                                    <h5 class="card-title ms-3"><%= movie.name %></h5>
                                                    <p class="card-text ms-3"> <%= movie.genre %> </p>
                                                    <p class="card-text ms-3"> <%= movie.rate %> </p>
                                                    <p class="card-text ms-3"> <%= movie.time %> </p>
                                                    <a href = "/movies/<%= movie._id %>" class = "btn btn-sm btn-outline-light ms-3" > VIEW </a>
                                                </div>
                                            </li>

                                            <div class="d-flex mt-3">
                                                <% for( let i = 0; cinema.allShowtime[i] != null; i++){ %> 
                                                    <form action="/select-seat/<%= cinema._id %>/<%= movie._id %>" method = "post">
                                                        <input type="hidden" name = "showtime" value="<%= cinema.allShowtime[i] %>">
                                                        <% let time = cinema.allShowtime[i].split(':') %>       
                                                        <% if( Number(time[0]) <= today.getHours() && Number(time[1]) <= today.getMinutes()){ %> 
                                                        <button type="submit" disabled class="btn card-text btn btn-outline-warning me-5 mt-2 mb-2"> <%= cinema.allShowtime[i] %></button>
                                                        <% } else { %>
                                                        <button type="submit" class="btn card-text btn btn-outline-warning me-5 mt-2 mb-2"> <%= cinema.allShowtime[i] %></button>
                                                        <% } %>
                                                        <input type="hidden" name = "date" value="<%= today.getFullYear() + '-' + Number(today.getMonth()+1) + '-' + today.getDate() %>"> 
                                                    </form>
                                                <% } %>
                                            </div>
                                        <% } %>
                                    <% }) %>
                                </ol>
                            </div>
                        <% } %>
                        <% } %> 
                <% }) %>

                <!-- cinema2 -->
                <% cinema.forEach(function(cinema){ %>
                    <% if( Date.now() >= Date.parse(cinema.startDate) &&  Date.now() <= Date.parse(cinema.endDate)) {%>
                        <% if(x === 1 ){ x = 2 %>
                                <h4 class="text-light ms-5"> Cinema 2 </h4>
                        <% } %>    
                        <% if( cinema.cinema === '2' ){ %> 
                            <div class="container">
                                <ol class="list-group w-50">
                                    <% movie.forEach(function(movie){ %>
                                        <% if( movie._id.equals(cinema.movieId._id)){ %>
                                            <li class="list-group-item d-flex justify-content-between align-items-start mt-3 bgAllPage text-light">
                                                <img src="<%= movie.poster %>" width="150" height="200">
                                                <div class="ms-2 me-auto mt-4">
                                                    <h5 class="card-title ms-3"><%= movie.name %></h5>
                                                    <p class="card-text ms-3"> <%= movie.genre %> </p>
                                                    <p class="card-text ms-3"> <%= movie.rate %> </p>
                                                    <p class="card-text ms-3"> <%= movie.time %> </p>
                                                    <a href = "/movies/<%= movie._id %>" class = "btn btn-sm btn-outline-light ms-3" > VIEW </a>
                                                </div>
                                            </li>

                                            <div class="d-flex mt-3">
                                                <% for( let i = 0; cinema.allShowtime[i] != null; i++){ %> 
                                                    <form action="/select-seat/<%= cinema._id %>/<%= movie._id %>" method = "post">
                                                        <input type="hidden" name = "showtime" value="<%= cinema.allShowtime[i] %>">
                                                        <% let time = cinema.allShowtime[i].split(':') %>       
                                                        <% if( Number(time[0]) <= today.getHours() && Number(time[1]) <= today.getMinutes()){ %> 
                                                        <button type="submit" disabled class="btn card-text btn btn-outline-warning me-5 mt-2 mb-2"> <%= cinema.allShowtime[i] %></button>
                                                        <% } else { %>
                                                        <button type="submit" class="btn card-text btn btn-outline-warning me-5 mt-2 mb-2"> <%= cinema.allShowtime[i] %></button>
                                                        <% } %>
                                                        <input type="hidden" name = "date" value="<%= today.getFullYear() + '-' + Number(today.getMonth()+1) + '-' + today.getDate() %>"> 
                                                    </form>
                                                <% } %>
                                            </div>
                                        <% } %>
                                    <% }) %>
                                </ol>
                            </div>
                        <% } %>
                        <% } %> 
                <% }) %>

                 <!-- cinema3 -->
                 <% cinema.forEach(function(cinema){ %>
                    <% if( Date.now() >= Date.parse(cinema.startDate) &&  Date.now() <= Date.parse(cinema.endDate)) {%>
                        <% if(x === 2 ){ x = 3 %>
                                <h4 class="text-light ms-5"> Cinema 3 </h4>
                        <% } %>    
                        <% if( cinema.cinema === '3' ){ %> 
                            <div class="container">
                                <ol class="list-group w-50">
                                    <% movie.forEach(function(movie){ %>
                                        <% if( movie._id.equals(cinema.movieId._id)){ %>
                                            <li class="list-group-item d-flex justify-content-between align-items-start mt-3 bgAllPage text-light">
                                                <img src="<%= movie.poster %>" width="150" height="200">
                                                <div class="ms-2 me-auto mt-4">
                                                    <h5 class="card-title ms-3"><%= movie.name %></h5>
                                                    <p class="card-text ms-3"> <%= movie.genre %> </p>
                                                    <p class="card-text ms-3"> <%= movie.rate %> </p>
                                                    <p class="card-text ms-3"> <%= movie.time %> </p>
                                                    <a href = "/movies/<%= movie._id %>" class = "btn btn-sm btn-outline-light ms-3" > VIEW </a>
                                                </div>
                                            </li>

                                            <div class="d-flex mt-3">
                                                <% for( let i = 0; cinema.allShowtime[i] != null; i++){ %> 
                                                    <form action="/select-seat/<%= cinema._id %>/<%= movie._id %>" method = "post">
                                                        <input type="hidden" name = "showtime" value="<%= cinema.allShowtime[i] %>">
                                                        <% let time = cinema.allShowtime[i].split(':') %>       
                                                        <% if( Number(time[0]) <= today.getHours() && Number(time[1]) <= today.getMinutes()){ %> 
                                                        <button type="submit" disabled class="btn card-text btn btn-outline-warning me-5 mt-2 mb-2"> <%= cinema.allShowtime[i] %></button>
                                                        <% } else { %>
                                                        <button type="submit" class="btn card-text btn btn-outline-warning me-5 mt-2 mb-2"> <%= cinema.allShowtime[i] %></button>
                                                        <% } %>
                                                        <input type="hidden" name = "date" value="<%= today.getFullYear() + '-' + Number(today.getMonth()+1) + '-' + today.getDate() %>"> 
                                                    </form>
                                                <% } %>
                                            </div>
                                        <% } %>
                                    <% }) %>
                                </ol>
                            </div>
                        <% } %>
                        <% } %> 
                <% }) %>

                 <!-- cinema4 -->
                 <% cinema.forEach(function(cinema){ %>
                    <% if( Date.now() >= Date.parse(cinema.startDate) &&  Date.now() <= Date.parse(cinema.endDate)) {%>
                        <% if(x === 3 ){ x = 4 %>
                                <h4 class="text-light ms-5"> Cinema 4 </h4>
                        <% } %>    
                        <% if( cinema.cinema === '4' ){ %> 
                            <div class="container">
                                <ol class="list-group w-50">
                                    <% movie.forEach(function(movie){ %>
                                        <% if( movie._id.equals(cinema.movieId._id)){ %>
                                            <li class="list-group-item d-flex justify-content-between align-items-start mt-3 bgAllPage text-light">
                                                <img src="<%= movie.poster %>" width="150" height="200">
                                                <div class="ms-2 me-auto mt-4">
                                                    <h5 class="card-title ms-3"><%= movie.name %></h5>
                                                    <p class="card-text ms-3"> <%= movie.genre %> </p>
                                                    <p class="card-text ms-3"> <%= movie.rate %> </p>
                                                    <p class="card-text ms-3"> <%= movie.time %> </p>
                                                    <a href = "/movies/<%= movie._id %>" class = "btn btn-sm btn-outline-light ms-3" > VIEW </a>
                                                </div>
                                            </li>

                                            <div class="d-flex mt-3">
                                                <% for( let i = 0; cinema.allShowtime[i] != null; i++){ %> 
                                                    <form action="/select-seat/<%= cinema._id %>/<%= movie._id %>" method = "post">
                                                        <input type="hidden" name = "showtime" value="<%= cinema.allShowtime[i] %>">
                                                        <% let time = cinema.allShowtime[i].split(':') %>       
                                                        <% if( Number(time[0]) <= today.getHours() && Number(time[1]) <= today.getMinutes()){ %> 
                                                        <button type="submit" disabled class="btn card-text btn btn-outline-warning me-5 mt-2 mb-2"> <%= cinema.allShowtime[i] %></button>
                                                        <% } else { %>
                                                        <button type="submit" class="btn card-text btn btn-outline-warning me-5 mt-2 mb-2"> <%= cinema.allShowtime[i] %></button>
                                                        <% } %>
                                                        <input type="hidden" name = "date" value="<%= today.getFullYear() + '-' + Number(today.getMonth()+1) + '-' + today.getDate() %>"> 
                                                    </form>
                                                <% } %>
                                            </div>
                                        <% } %>
                                    <% }) %>
                                </ol>
                            </div>
                        <% } %>
                        <% } %> 
                <% }) %>

                 <!-- cinema5 -->
                <% cinema.forEach(function(cinema){ %>
                    <% if( Date.now() >= Date.parse(cinema.startDate) &&  Date.now() <= Date.parse(cinema.endDate)) {%>
                        <% if(x === 4 ){ x = 5 %>
                                <h4 class="text-light ms-5"> Cinema 5 </h4>
                        <% } %>    
                        <% if( cinema.cinema === '5' ){ %> 
                            <div class="container">
                                <ol class="list-group w-50">
                                    <% movie.forEach(function(movie){ %>
                                        <% if( movie._id.equals(cinema.movieId._id)){ %>
                                            <li class="list-group-item d-flex justify-content-between align-items-start mt-3 bgAllPage text-light">
                                                <img src="<%= movie.poster %>" width="150" height="200">
                                                <div class="ms-2 me-auto mt-4">
                                                    <h5 class="card-title ms-3"><%= movie.name %></h5>
                                                    <p class="card-text ms-3"> <%= movie.genre %> </p>
                                                    <p class="card-text ms-3"> <%= movie.rate %> </p>
                                                    <p class="card-text ms-3"> <%= movie.time %> </p>
                                                    <a href = "/movies/<%= movie._id %>" class = "btn btn-sm btn-outline-light ms-3" > VIEW </a>
                                                </div>
                                            </li>

                                            <div class="d-flex mt-3">
                                                <% for( let i = 0; cinema.allShowtime[i] != null; i++){ %> 
                                                    <form action="/select-seat/<%= cinema._id %>/<%= movie._id %>" method = "post">
                                                        <input type="hidden" name = "showtime" value="<%= cinema.allShowtime[i] %>">
                                                        <% let time = cinema.allShowtime[i].split(':') %>       
                                                        <% if( Number(time[0]) <= today.getHours() && Number(time[1]) <= today.getMinutes()){ %> 
                                                        <button type="submit" disabled class="btn card-text btn btn-outline-warning me-5 mt-2 mb-2"> <%= cinema.allShowtime[i] %></button>
                                                        <% } else { %>
                                                        <button type="submit" class="btn card-text btn btn-outline-warning me-5 mt-2 mb-2"> <%= cinema.allShowtime[i] %></button>
                                                        <% } %>
                                                        <input type="hidden" name = "date" value="<%= today.getFullYear() + '-' + Number(today.getMonth()+1) + '-' + today.getDate() %>"> 
                                                    </form>
                                                <% } %>
                                            </div>
                                        <% } %>
                                    <% }) %>
                                </ol>
                            </div>
                        <% } %>
                        <% } %> 
                <% }) %>

                <!-- cinema 6 -->
                <% cinema.forEach(function(cinema){ %>
                    <% if( Date.now() >= Date.parse(cinema.startDate) &&  Date.now() <= Date.parse(cinema.endDate)) {%>
                        <% if(x === 5 ){ x = 6 %>
                                <h4 class="text-light ms-5"> Cinema 6 </h4>
                        <% } %>    
                        <% if( cinema.cinema === '6' ){ %> 
                            <div class="container">
                                <ol class="list-group w-50">
                                    <% movie.forEach(function(movie){ %>
                                        <% if( movie._id.equals(cinema.movieId._id)){ %>
                                            <li class="list-group-item d-flex justify-content-between align-items-start mt-3 bgAllPage text-light">
                                                <img src="<%= movie.poster %>" width="150" height="200">
                                                <div class="ms-2 me-auto mt-4">
                                                    <h5 class="card-title ms-3"><%= movie.name %></h5>
                                                    <p class="card-text ms-3"> <%= movie.genre %> </p>
                                                    <p class="card-text ms-3"> <%= movie.rate %> </p>
                                                    <p class="card-text ms-3"> <%= movie.time %> </p>
                                                    <a href = "/movies/<%= movie._id %>" class = "btn btn-sm btn-outline-light ms-3" > VIEW </a>
                                                </div>
                                            </li>

                                            <div class="d-flex mt-3">
                                                <% for( let i = 0; cinema.allShowtime[i] != null; i++){ %> 
                                                    <form action="/select-seat/<%= cinema._id %>/<%= movie._id %>" method = "post">
                                                        <input type="hidden" name = "showtime" value="<%= cinema.allShowtime[i] %>">
                                                        <% let time = cinema.allShowtime[i].split(':') %>       
                                                        <% if( Number(time[0]) <= today.getHours() && Number(time[1]) <= today.getMinutes()){ %> 
                                                        <button type="submit" disabled class="btn card-text btn btn-outline-warning me-5 mt-2 mb-2"> <%= cinema.allShowtime[i] %></button>
                                                        <% } else { %>
                                                        <button type="submit" class="btn card-text btn btn-outline-warning me-5 mt-2 mb-2"> <%= cinema.allShowtime[i] %></button>
                                                        <% } %>
                                                        <input type="hidden" name = "date" value="<%= today.getFullYear() + '-' + Number(today.getMonth()+1) + '-' + today.getDate() %>"> 
                                                    </form>
                                                <% } %>
                                            </div>
                                        <% } %>
                                    <% }) %>
                                </ol>
                            </div>
                        <% } %>
                        <% } %> 
                <% }) %>

                <!-- cinema 7 -->
                <% cinema.forEach(function(cinema){ %>
                    <% if( Date.now() >= Date.parse(cinema.startDate) &&  Date.now() <= Date.parse(cinema.endDate)) {%>
                        <% if(x === 6 ){ x = 7 %>
                                <h4 class="text-light ms-5"> Cinema 7 </h4>
                        <% } %>    
                        <% if( cinema.cinema === '7' ){ %> 
                            <div class="container">
                                <ol class="list-group w-50">
                                    <% movie.forEach(function(movie){ %>
                                        <% if( movie._id.equals(cinema.movieId._id)){ %>
                                            <li class="list-group-item d-flex justify-content-between align-items-start mt-3 bgAllPage text-light">
                                                <img src="<%= movie.poster %>" width="150" height="200">
                                                <div class="ms-2 me-auto mt-4">
                                                    <h5 class="card-title ms-3"><%= movie.name %></h5>
                                                    <p class="card-text ms-3"> <%= movie.genre %> </p>
                                                    <p class="card-text ms-3"> <%= movie.rate %> </p>
                                                    <p class="card-text ms-3"> <%= movie.time %> </p>
                                                    <a href = "/movies/<%= movie._id %>" class = "btn btn-sm btn-outline-light ms-3" > VIEW </a>
                                                </div>
                                            </li>

                                            <div class="d-flex mt-3">
                                                <% for( let i = 0; cinema.allShowtime[i] != null; i++){ %> 
                                                    <form action="/select-seat/<%= cinema._id %>/<%= movie._id %>" method = "post">
                                                        <input type="hidden" name = "showtime" value="<%= cinema.allShowtime[i] %>">
                                                        <% let time = cinema.allShowtime[i].split(':') %>       
                                                        <% if( Number(time[0]) <= today.getHours() && Number(time[1]) <= today.getMinutes()){ %> 
                                                        <button type="submit" disabled class="btn card-text btn btn-outline-warning me-5 mt-2 mb-2"> <%= cinema.allShowtime[i] %></button>
                                                        <% } else { %>
                                                        <button type="submit" class="btn card-text btn btn-outline-warning me-5 mt-2 mb-2"> <%= cinema.allShowtime[i] %></button>
                                                        <% } %>
                                                        <input type="hidden" name = "date" value="<%= today.getFullYear() + '-' + Number(today.getMonth()+1) + '-' + today.getDate() %>"> 
                                                    </form>
                                                <% } %>
                                            </div>
                                        <% } %>
                                    <% }) %>
                                </ol>
                            </div>
                        <% } %>
                        <% } %> 
                <% }) %>

                <!-- cinema 8 -->
                <% cinema.forEach(function(cinema){ %>
                    <% if( Date.now() >= Date.parse(cinema.startDate) &&  Date.now() <= Date.parse(cinema.endDate)) {%>
                        <% if(x === 7 ){ x = 8 %>
                                <h4 class="text-light ms-5"> Cinema 8 </h4>
                        <% } %>    
                        <% if( cinema.cinema === '8' ){ %> 
                            <div class="container">
                                <ol class="list-group w-50">
                                    <% movie.forEach(function(movie){ %>
                                        <% if( movie._id.equals(cinema.movieId._id)){ %>
                                            <li class="list-group-item d-flex justify-content-between align-items-start mt-3 bgAllPage text-light">
                                                <img src="<%= movie.poster %>" width="150" height="200">
                                                <div class="ms-2 me-auto mt-4">
                                                    <h5 class="card-title ms-3"><%= movie.name %></h5>
                                                    <p class="card-text ms-3"> <%= movie.genre %> </p>
                                                    <p class="card-text ms-3"> <%= movie.rate %> </p>
                                                    <p class="card-text ms-3"> <%= movie.time %> </p>
                                                    <a href = "/movies/<%= movie._id %>" class = "btn btn-sm btn-outline-light ms-3" > VIEW </a>
                                                </div>
                                            </li>

                                            <div class="d-flex mt-3">
                                                <% for( let i = 0; cinema.allShowtime[i] != null; i++){ %> 
                                                    <form action="/select-seat/<%= cinema._id %>/<%= movie._id %>" method = "post">
                                                        <input type="hidden" name = "showtime" value="<%= cinema.allShowtime[i] %>">
                                                        <% let time = cinema.allShowtime[i].split(':') %>       
                                                        <% if( Number(time[0]) <= today.getHours() && Number(time[1]) <= today.getMinutes()){ %> 
                                                        <button type="submit" disabled class="btn card-text btn btn-outline-warning me-5 mt-2 mb-2"> <%= cinema.allShowtime[i] %></button>
                                                        <% } else { %>
                                                        <button type="submit" class="btn card-text btn btn-outline-warning me-5 mt-2 mb-2"> <%= cinema.allShowtime[i] %></button>
                                                        <% } %>
                                                        <input type="hidden" name = "date" value="<%= today.getFullYear() + '-' + Number(today.getMonth()+1) + '-' + today.getDate() %>"> 
                                                    </form>
                                                <% } %>
                                            </div>
                                        <% } %>
                                    <% }) %>
                                </ol>
                            </div>
                        <% } %>
                        <% } %> 
                <% }) %>
        <% } %> 
<%- include( "../partials/footer.ejs" ) %>